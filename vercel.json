{
  "buildCommand": "cd .. && bun install --frozen-lockfile && bun run build:sdk && cd app && bun run build",
  "outputDirectory": "dist",
  "installCommand": "bun install --frozen-lockfile",
  "framework": null
}
